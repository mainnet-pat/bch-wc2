{
  "name": "@bch-wc2/root",
  "private": true,
  "version": "0.0.13",
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "devDependencies": {},
  "scripts": {
    "bump": "node bump_version.cjs",
    "publish": "yarn workspaces run publish:public",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  }
}
